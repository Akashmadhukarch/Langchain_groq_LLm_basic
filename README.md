# Langchain_groq_LLm_basic
# LangChain + Groq LLM Setup

This project demonstrates how to use LangChain with the Groq API
to generate AI responses using Python.

It connects:

Python → LangChain → Groq API → LLM Response

---

 📌 Requirements

- Python 3.10.x (Recommended)
- Virtual Environment
- Groq API Key
- Internet Connection

---

 📦 Installation

 1️⃣ Create Virtual Environment

```bash
python -m venv langchain_env
Windows:
langchain_env\Scripts\activate
2️⃣ Install Required Packages
pip install langchain groq langchain-community
▶️ Running the Application
Activate environment:langchain_env\Scripts\activate
Run the script:
python LangChain01_converted.py


⚠️ Important Notes

Do NOT hardcode your API key.

Always use environment variables.

Python 3.10 is recommended for best compatibility.


NOTE:  just aa learning phase demo 
